{
  "name": "cookie-reaper",
  "version": "2.0.0",
  "description": "Advanced cookie replaying tool for perfect device emulation from stealer logs",
  "scripts": {
    "build": "node build.js",
    "package": "node build.js --package"
  },
  "devDependencies": {
    "archiver": "^6.0.2"
  }
}
